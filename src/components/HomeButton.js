import React from "react";
import { StyledButton, StyledLinks } from "./StyledComponents";

export default ()=>{
    return (
        <StyledLinks to="/"><StyledButton >Home Page</StyledButton></StyledLinks>
    );
};